---
import PageLayout from '../../layouts/PageLayout.astro';
import SectionHeading from '../../components/ui/SectionHeading.astro';

const brands = [
	{ name: 'Savant', logo: '/images/brands/logos/savant.svg', href: '/brands/savant-2/' },
	{ name: 'Control4', logo: '/images/brands/logos/control4.webp', href: '/brands/control4/' },
	{ name: 'Lutron', logo: '/images/brands/logos/lutron.webp', href: '/brands/lutron-2/' },
	{ name: 'Crestron', logo: '/images/brands/logos/crestron.webp', href: '/brands/crestron/' },
	{ name: 'Basalte', logo: '/images/brands/logos/basalte.webp', href: '/brands/basalte/' },
	{ name: 'Josh.ai', logo: '/images/brands/logos/josh-ai.svg', href: '/brands/josh-ai-2/' },
];
---

<PageLayout
	title="Our Brand Partners"
	description="PROJECT: automate partners with the world's leading smart home brands — Savant, Control4, Lutron, Crestron, Basalte, and Josh.ai."
>
	<section class="section">
		<div class="container">
			<SectionHeading
				eyebrow="Brands"
				title="Best-in-Class Technology Partners"
				subtitle="We design and install systems built on the industry's most trusted smart home platforms."
				align="center"
			/>

			<div class="grid">
				{brands.map((brand) => (
					<a class="brand-card" href={brand.href}>
						<img src={brand.logo} alt={`${brand.name} logo`} loading="lazy" />
					</a>
				))}
			</div>
		</div>
	</section>
</PageLayout>

<style>
	.grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-6);
	}

	.brand-card {
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--color-white);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-md);
		padding: var(--space-12) var(--space-8);
		transition: border-color var(--transition-base), transform var(--transition-base);
	}

	.brand-card:hover {
		border-color: var(--color-accent);
		transform: translateY(-4px);
	}

	.brand-card img {
		max-height: 60px;
		width: auto;
	}

	@media (max-width: 700px) {
		.grid {
			grid-template-columns: 1fr;
		}
	}
</style>
