<section class="ss-hero">
	<div class="container hero-grid">
		<div class="hero-head">
			<span class="eyebrow">Success Stories</span>
			<h1>Selected Residences Across Los Angeles</h1>
		</div>
		<p class="hero-lede">
			Explore PROJECT: automate residences where thoughtful design and intelligent technology
			work together effortlessly, from the Malibu coast to the South Bay.
		</p>
	</div>
</section>

<style>
	.ss-hero {
		padding-block: clamp(4rem, 10vw, 8rem) clamp(2.5rem, 6vw, 4.5rem);
	}

	.hero-grid {
		display: grid;
		grid-template-columns: minmax(0, 7fr) minmax(0, 5fr);
		gap: var(--space-6) clamp(2rem, 6vw, 6rem);
		align-items: end;
	}

	.hero-head > *,
	.hero-lede {
		animation: ss-rise 1100ms var(--ease-out-expo) both;
	}

	.hero-head h1 {
		margin: 0;
		font-size: var(--text-display-lg);
		font-weight: var(--fw-medium);
		line-height: 1.08;
		letter-spacing: -0.015em;
		color: var(--color-text);
		text-wrap: balance;
		animation-delay: 90ms;
	}

	.hero-lede {
		margin: 0;
		max-width: 32rem;
		font-size: var(--text-body-lg);
		line-height: 1.65;
		animation-delay: 180ms;
	}

	@keyframes ss-rise {
		from {
			opacity: 0;
			transform: translateY(18px);
		}
		to {
			opacity: 1;
			transform: none;
		}
	}

	@media (max-width: 900px) {
		.hero-grid {
			grid-template-columns: minmax(0, 1fr);
			align-items: start;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.hero-head > *,
		.hero-lede {
			animation: none;
		}
	}
</style>
