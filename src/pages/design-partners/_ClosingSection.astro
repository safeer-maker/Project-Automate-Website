---
import { withBase } from '../../lib/paths.ts';
---

<section class="closing" aria-labelledby="dp-closing-heading">
	<div class="container closing-inner">
		<span class="eyebrow">Let's Start Your Journey</span>
		<h2 id="dp-closing-heading">Step into the future with PROJECT: automate.</h2>
		<p>Access smart solutions, expert knowledge, and a network that drives success.</p>
		<a class="closing-link" href={withBase('/success-stories/')}>
			See selected residences
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
				<path d="M5 12h14M13 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round" />
			</svg>
		</a>
	</div>
</section>

<style>
	.closing {
		padding-block: clamp(4.5rem, 10vw, 8rem);
		border-top: 1px solid var(--color-border);
	}

	.closing-inner {
		max-width: 44rem;
		text-align: center;
	}

	.closing h2 {
		margin: 0 0 var(--space-6);
		font-size: var(--text-display-md);
		font-weight: var(--fw-medium);
		line-height: 1.12;
		letter-spacing: -0.015em;
		color: var(--color-text);
		text-wrap: balance;
	}

	.closing p {
		margin: 0 auto var(--space-8);
		max-width: 32rem;
		font-size: var(--text-body-lg);
		line-height: 1.7;
	}

	.closing-link {
		display: inline-flex;
		align-items: center;
		gap: var(--space-3);
		padding-bottom: var(--space-1);
		border-bottom: 1px solid currentColor;
		font-weight: var(--fw-medium);
		color: var(--color-accent);
		transition: color var(--dur-fast) ease;
	}

	.closing-link svg {
		width: 18px;
		height: 18px;
		transition: transform var(--dur-base) var(--ease-out-expo);
	}

	.closing-link:hover,
	.closing-link:focus-visible {
		color: var(--color-accent-hover);
	}

	.closing-link:hover svg,
	.closing-link:focus-visible svg {
		transform: translateX(5px);
	}

	@media (prefers-reduced-motion: reduce) {
		.closing-link svg {
			transition: none;
		}
	}
</style>
